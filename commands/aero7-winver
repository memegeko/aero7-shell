#!/usr/bin/env bash
set -Eeuo pipefail

message=$'Aero7\nClassic Aero desktop\nRunning on Arch Linux\n\nAero7 is an independent, free and open-source project.'

if command -v linver >/dev/null 2>&1; then
  exec linver
elif command -v kdialog >/dev/null 2>&1; then
  exec kdialog --title "Aero7-shell" --msgbox "$message"
elif command -v zenity >/dev/null 2>&1; then
  exec zenity --info --title "Aero7-shell" --text "$message"
else
  printf '%s\n' "$message"
fi
